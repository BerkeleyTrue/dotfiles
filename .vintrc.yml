cmdargs:
  # Checking more strictly
  severity: style_problem

  # Enable coloring
  color: false

  # Enable Neovim syntax
  env:
    neovim: true

policies:
  # Disable a violation
  ProhibitMissingScriptEncoding:
    enabled: false
