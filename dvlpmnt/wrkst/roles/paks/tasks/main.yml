---
- name: update
  command: flatpak update --noninteractive
  changed_when: true
  tags:
    - update
    - flatpak
    - flatpak-update

- name: install
  flatpak:
    name: "{{ item.name }}"
    remote: "{{ item.remote | default('flathub') }}"
    state: present
  loop: "{{ paks }}"
  tags:
    - packages
    - flatpak
