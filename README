git clone folder in home directory.
The file directory will be 

~/.vim 

While in your home directory enter the following,

ln -s ~/dotfiles/vimrc ~/.vimrc 
ln -s ~/dotfiles/bash_aliases ./.bash_aliases
 
Now move into 

 ~/.vim/bundle 
 
And perform the following,

git submodule init 
git submodule update 

This will initialize and download the submodules.
But wait! pyFlakes has its own submodule.
So,

cd ~/.vim/bundle/pyflakes 

Then  

git submodule init 
git submodule update 

A summery of what is in the bundle folder

Command-T:
Delmitmate:
OmniCppComplete:
pyFlakes: 
Rainbow_Parentheses:
solarized:Provided color for vim editor.
supertab:
vim-LaTeX: LaTeX bindings for document generation

TODO: Complete descriptions of the included vim files
